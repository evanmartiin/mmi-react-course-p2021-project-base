import React from 'react'
import Layout from '../../components/Layout/Layout'

const Timer = () => {
    return (
        <div>
            <Layout>
                Minuteur
            </Layout>
        </div>
    );
}

export default Timer